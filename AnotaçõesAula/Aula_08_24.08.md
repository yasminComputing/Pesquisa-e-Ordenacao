## Aula 08 - Dia 24.08.2026

> Prova teória: importancia de comparacao, o que são codificacao simples, o que é organizando lista e metodos, (compareTo), como se sabe que o algoritmo tem maior complexidade e menor complexidade (comparacoes de trocas) .
Quando comparada f1 com f1 dá zero, por conta do caracteres. Quando comparado f1 e f2 dá -15 e inverter a ordem dá 15. 
```java
String f1 = "Alexandre";
      String f2 = "Pietro";
        System.out.println(f1.compareTo(f1));
```

Quando construir o MVC já deixar o getters e setters e o equals e já fazer o compareTo. 

Código desenvolvido durante à aula. 

```java
package javaapplication1;

import java.util.Objects;

public class Pessoa implements Comparable<Pessoa> {
    private String nome;
    private int idade;
    /***
     * Construtor
     * @param nome
     * @param idade 
     */
    public Pessoa(String nome, int idade) {
        this.nome = nome;
        this.idade = idade;
    }

    public String getNome() {
        return nome;
    }

    public void setNome(String nome) {
        this.nome = nome;
    }

    public int getIdade() {
        return idade;
    }

    public void setIdade(int idade) {
        this.idade = idade;
    }

    @Override
    public String toString() {
        return  "nome = " + nome + " Idade = " + idade + " ";
    }

    @Override
    public int hashCode() {
        int hash = 7;
        hash = 89 * hash + Objects.hashCode(this.nome);
        hash = 89 * hash + this.idade;
        return hash;
    }

    @Override
    public boolean equals(Object obj) {
        if (this == obj) {
            return true;
        }
        if (obj == null) {
            return false;
        }
        if (getClass() != obj.getClass()) {
            return false;
        }
        final Pessoa other = (Pessoa) obj;
        if (this.idade != other.idade) {
            return false;
        }
        return Objects.equals(this.nome, other.nome);
        
        
    }

    @Override
    public int compareTo(Pessoa o) {
          int resultadoNome = this.nome.compareTo(o.getNome());
          if(resultadoNome != 0){
              return resultadoNome;
          }
          return Integer.compare(this.idade,o.getIdade());
    }    
}

```


```java
package javaapplication1;

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

public class Principal {

    public static void main(String[] args) {
       List<Pessoa> lista = new ArrayList<>();
        
       lista.add(new Pessoa("Gabriel",19));
       lista.add(new Pessoa("Alex",52));
       lista.add(new Pessoa("Bernardo",6));
       lista.add(new Pessoa("Bernardo",30));
       lista.add(new Pessoa("Gabriel",20));
       lista.add(new Pessoa("Alex",51));
       
       
     /* Collections.sort(lista);
      
      System.out.println(lista);*/
     
      //relatorio temporario ordenar por idade ordem decrescente(e para crescente so mudar a ordem do p1 e p2 usando funcao lamba
      lista.sort((p1, p2) -> Integer.compare(p1.getIdade(), p2.getIdade()));
      
        System.out.println(lista);
    }
}
```